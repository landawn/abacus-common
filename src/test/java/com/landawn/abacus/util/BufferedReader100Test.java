package com.landawn.abacus.util;

import java.io.IOException;

import org.junit.jupiter.api.Test;

import com.landawn.abacus.TestBase;

public class BufferedReader100Test extends TestBase {

    @Test
    public void testConstructorWithString() throws IOException {
        // Note: BufferedReader is package-private, so we cannot directly test it
        // Testing would require reflection or making it public
    }

    // Note: BufferedReader is package-private class, 
    // so direct testing is limited without using reflection
}